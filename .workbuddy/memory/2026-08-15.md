# 2026-08-15 工作记录

## 证书 PDF 转图并接入
- 上次 PyMuPDF 安装"失败"实为假失败：包已装在 `C:/Users/Admin/.workbuddy/binaries/python/envs/default/`，但 `python3`(3.13.14) 指向另一环境，故 `import fitz` 报缺模块。用该 env 的 `Scripts/python.exe` 运行即可。该 env 缺 PIL，先 `pip install pillow` 补齐。
- 渲染脚本（PyMuPDF + PIL）：长边 1600px、对比度 +5%、JPEG 质量 88、progressive。
- 产出：`assets/images/certs/gaoqi-certificate.jpg`(1600×1084 / 169KB)、`iso9001-qms-certificate.jpg`(1129×1600 / 262KB)。原件 PDF 仍保留于 `assets/files/certs/`。
- about.html 资质区「证书原件」由纯 PDF 文字链接升级为图片卡片（`<figure>`+`<img>`），hover 轻微放大，点击查看 PDF 原件；`pc.css` 重写 `.cert-doc` 系列样式为图片卡片（`mobile.css` 单列已适配）。
- 预览验证：http://localhost:8099/pages/about.html 及两张图片均 HTTP 200，服务器 8099 在线。

## 首页重构为长落地页
- 需求：首页要"长落地页"，让客户一眼看懂"我们是干什么的 + 产品特点"。
- 动线设计：Hero 定位 → 数据条 → 痛点区(深色,三座大山) → 原理图文(变频电磁场/方解石变文石态) → 核心产品(三款机柜+特点清单) → 四大特色 → 应用场景+行业标签云 → 客户信任(资质证书图+9案例标签) → 设备监测入口(DTU转TCP,呼应项目预留模块) → 服务流程(4步) → CTA → 页脚。
- 全部文案取自真实素材（技术原理/产品参数/案例/资质/节能数据），未虚构数字。
- 新增 CSS（pc.css）：`.section--dark` 内浅色标题/白字卡片适配、`.card__points`、`.chip`/`.chip-row`/`.chip--solid`、`.link-more`、`.monitor-entry`、`.process-steps`。mobile.css：`.grid--3/.grid--2` 单列、`.process-steps` 两列、`.monitor-entry` 堆叠、`.chip-row` 左对齐。
- 复用既有 `.section--dark`(common.css 已定义)、`.cert-docs/.cert-doc`、`.about-intro`。
- 校验：标签全闭合(section 11/11, article 5/5, figure 2/2, div 82/82)，首页+图片+CSS+PDF 均 HTTP 200。服务器 8099 重启后在线。
- 注：`python3` 指向的环境无 `fitz/PIL`；PDF 渲染须用 `C:/Users/Admin/.workbuddy/binaries/python/envs/default/Scripts/python.exe`。

## 专利墙（12 项专利）
- 用户提供 12 份专利证书 PDF（F:/思谋思资料/资质文件/知识产权/），要求做成"专利墙"展示，缩略图可不清楚（点开看原件）。
- 归档：原件 PDF → `assets/files/patents/`(英文重命名 patent-01..12-*.pdf)；缩略图用 PyMuPDF 渲染（长边 560px、JPEG 质量 75，约 26KB/张）→ `assets/images/patents/patent-0X-*.jpg`。渲染时 MuPDF 报 `BJCA:XmlData` 警告无害。
- about.html「资质荣誉」区在「证书原件」后新增专利墙：`.patent-wall` grid（PC 4列 / 移动 2列），每卡含缩略图(轻微 blur，hover 变清晰)+名称+授权年份+「查看原件 PDF」链接（每卡 2 个 PDF 链接）。
- CSS：pc.css 新增 `.patent-wall/-head/-title/-desc/-card/-card__media/-card__body/-card__name/-card__year/-card__hint`；mobile.css 媒体查询内追加 `.patent-wall` 2列 + `.patent-card__name` 取消 min-height。
- 校验：12 缩略图 + 12 PDF 齐全；about.html 标签闭合 OK（figure 14/14 含 12 专利卡+2 证书卡）；HTTP 200。

## 待办（延续）
- 下批素材：解决方案场景照、厂房/团队照、监测页素材、新闻缩略图 7 张、地图/二维码/favicon/OG 图。
- P0 文案：monitor 去演示化、4 页 SEO 补词、9 页页脚统一、title 分隔符统一。
- 后台编辑系统、真实域名替换、联系方式核对、详情页模板。
