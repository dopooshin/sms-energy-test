
## 首页按豆包风格重做（2026-08-21）
- 用户要求：总结豆包 about.html 风格，并用该风格重做首页。
- 豆包风格基因：深海军蓝#0a1a2f主调、全屏Hero(背景图slowZoom+深蓝渐变遮罩+网格线+fadeUp错峰)、宽字距大标题、三段式hover翻蓝卡、展示区红色光晕+悬停放大、IntersectionObserver滚动渐入、固定导航下滚变磨砂深蓝。
- 落地：pages/index.html 重写为自包含内联CSS（与about页同源），保留全部11区块+SEO元数据+统一导航/页脚/schema。品牌红用#D60A1C（替代豆包#c8102e），标题统一Noto Sans SC 900（不再用宋体，与全站一致）。新增"现场实录"展示区（showcase-cooling/turbine.jpg，复用about素材）。移动端单列+汉堡菜单。
- 注意架构：index/about现已改为单文件内联样式，与products/solutions等仍用common+pc+mobile.css的旧架构不一致。后续若全站统一需迁移或接受差异化。
- 部署：edgeone pages deploy -n sms-energy-preview 重新部署，token刷新。预览验证首页/hero图/展示图均200。

## 22:10 Logo 全站最终统一
- 用户要求：真实 logo + 导航文字统一。最终定为「思谋思能源科技」+ 小字「SMOOTH ENERGY TEC.CO」，且旁配真实红色 SMS 字母标 logo-sms.png。
- 清理了之前漏掉的位置：页脚公司 logo 的 alt="思谋思 SMS Logo" → "思谋思能源科技 Logo"；JSON-LD 品牌名 "思谋思 SMS" → "思谋思能源科技"(products/technology 两页)。
- 用 python 批量替换 8 页，残留归零。已同步 deploy/preview/ 并重新 edgeone 部署刷新链接。
- 关键坑重申：之前用户看到的「几个页面 logo 不对」其实是 EdgeOne 预览还在跑老部署版本；本地 8 页早已统一，必须 sync 部署目录 + 重部署才反映到线上链接。

## 22:17 Logo 位置/大小/字体/颜色全站统一（最终根因修复）
- 根因：8 页分两套主题——index+products等5页用 atmos.css（有 .logo 样式），about/news/cases 三页用 common.css（旧 .header__logo 类名），而 HTML 已被改成 .logo 类 → 这三页 logo 完全无 CSS 接管，大小/字体/颜色不一致。
- 修复：把 common.css 里已无页面使用的旧 .header__logo* 块，替换为与 atmos.css 完全一致的 .logo/.logo__img/.logo-text/.logo-text small（值：图高38px、文字14px/700/字距2px/白、small 9px/rgba白.55）。atmos 移动端不改 logo 尺寸，故无需媒体查询覆盖。
- 现 8 页导航 logo 标记与样式 100% 统一；页脚 logo 本就是同一无 class img，已一致。
- 部署：sync deploy/preview + 重部署刷新链接（task Oblkwe）。
- 经验：改全站类名时必须同时确认每套主题的 CSS 都覆盖到；about/news/cases 虽是深蓝内容但 CSS 仍挂 common.css，后续若要彻底统一主题需迁到 atmos.css。

## 22:42 产品内页批量建设（仅本地，未上传）
- 需求：每个产品做统一结构的内页长页，含原理简介+优势+营销能力；无实拍产品用官网素材裁图。
- 6 个产品：分布式电磁阻垢机柜(小400×250×500/中600×322×870/大610×575×1523)、电磁节能器、循环水除垢设备、冷却塔填料除垢装置。
- 生成裁图 assets/images/products/{cabinet-small,mid,large,energy-saver,circulating-water,cooling-tower}.jpg（1600x1000 cover，从thumb/cabinet/showcase-turbine/showcase-cooling/cabinet-with-pipe-installation 裁出）；energy-saver 来自汽轮机图、circulating 来自机柜管装图、cooling-tower 来自冷却塔图。
- atmos.css 新增组件：.pd-hero(深蓝双栏产品Hero)、.pd-note(参数免责小字)、.faq/.faq-item(details/summary原生折叠)、.showcase 内标题转白；含移动端单列适配(@media 900px)。
- 6 个内页 pages/pd-*.html 统一结构：pd-hero → 核心参数stats-bar → 概述split → 原理split(配descaling-principle-diagram) → 核心优势grid-3(6卡) → 技术参数cmp-table(含*"以选型为准"免责) → 应用场景chip-row → 现场showcase → FAQ(5条) → 相关产品grid-3(互链) → CTA → 页脚 → Product schema。营销向：勾选式卖点、数据条、FAQ 异议处理、相关产品交叉引流。
- products.html 6 张卡片按钮全部接入对应 pd-*.html（修复了一处误入的"evidently"笔误）。
- 结构校验：6 页均 section 11/11、article 3/3、details 5/5 闭合，6 张产品图引用全部 OK。
- 注意：用户要求"只改本地、先不上传"，故未执行 edgeone deploy；仅同步 deploy/preview 本地暂存。
