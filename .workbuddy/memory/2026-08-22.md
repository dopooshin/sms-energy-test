# 2026-08-22 工作记录

## 官网后台管理系统（admin/）完整搭建
- 形态：轻量自建后台，Node.js + Express + 纯JS依赖（无原生编译），JSON 文件数据层（admin/data/*.json），EJS 模板。
- 三级角色 RBAC：super/editor/viewer，权限点模型 + 服务端 requireAbility 校验 + 前端按角色隐藏导航。演示账号 admin/admin123、editor/editor123、viewer/viewer123（npm run seed 初始化）。
- 模块：登录/登出、仪表盘、全局站点设置（导航/页脚/主题色/ICP/SEO默认，发布写 assets/site-settings.json）、用户与角色管理（仅超管，保留≥1超级管理员）、备份中心（一键备份/自动备份 node-cron/恢复前自动安全备份/历史下载）、操作日志（审计）。
- 验证：登录跳转、访客访问 settings/backup 返回403、logs 200；一键备份生成 zip（data+pages+assets）；设置发布写入 site-settings.json；自动策略保存成功。
- 注意：multer 1.x 有 audit 警告（本地管理可接受）；一键备份含 assets 图片约180MB，自动备份 keep 不宜过大；EdgeOne 为静态托管，后台需独立 Node 运行时才可上线。
- 运行：cd admin && npm install && npm run seed && npm start（默认3000端口）。
