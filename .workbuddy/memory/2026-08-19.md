
## EdgeOne Pages 临时预览部署（续 08-18）
- 部署命令：`cd deploy/preview && edgeone pages deploy -n sms-energy-preview`（国内站，PAGES_SOURCE=skills）。
- 项目ID makers-msj2popfvgfm，域名 sms-energy-preview-1dmqj3rp.edgeone.cool。
- ⚠️ 预览链接带 eo_token/eo_time 鉴权参数，**每次部署刷新 token，有效期约3小时(Max-Age=10800)**；过期后访问401，需重部署刷新。
- 根 index.html 必须带 location.search 跳转，否则落地页丢 token 会401（已修）。
- 验证坑：curl `-c/-b` cookie jar 不持久化 Partitioned cookie，导致资源请求一直401；实际浏览器正常。正确验证法：抓 302 的 Set-Cookie 头，用 `-H "Cookie: ..."` 手动带去请求页面/资源，确认 200+真实字节数。
- 正式长期对外需绑已备案自定义域名，解禁401限制。
